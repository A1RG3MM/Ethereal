<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ethereum</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');

        :root {
            --bg-color: #050505;
        }

        * {
            font-family: 'Montserrat', sans-serif;
        }

        body {
            background-color: var(--bg-color);
            color: #FFF;
        }

        .banner {
            position: fixed;
            bottom: 20px;
            right: 20px !important;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 10px;
            border-radius: 10px;
        }

        .banner-text {
            margin-right: 10px;
        }

        .banner-logo img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            padding-left: -30%;
        }

        ::-webkit-scrollbar {
            width: 0.4rem;
            height: 0.5rem;
        }

        ::-webkit-scrollbar-track {
            background: transparent;
            width: 0.4rem;
            height: 0.5rem;
        }

        ::-webkit-scrollbar-thumb {
            background: #070707;
            border-radius: 5px;
            width: 0.4rem;
            height: 0.5rem;
        }

        .chat-div {
            display: flex;
            justify-content: center;
            flex-direction: column;
            margin-right: 0;
            margin-left: auto;
            background-color: #FFF;
            width: 80vw;
        }

        .chat-history {
            background-color: #101010;
            position: fixed;
            top: 0;
            left: 0;
            bottom: 0;
            width: 17.5vw;
            height: 100vh;
        }

        .chat-history .align-top {
            position: absolute;
            top: 5px;
            left: 10px;
        }

        .buttons {
            width: 100%;
            overflow: auto;
            max-height: calc(100vh - 4.5em);
        }

        .buttons button {
            background-color: #1c1c1c;
            color: #FFF;
            border: none;
            width: calc(100% - 20px);
            padding: 10px;
            text-align: left;
            display: block;
            margin: 5px 10px;
        }

        .chat-history button {
            background-color: transparent;
            color: #FFF;
            border-radius: 5px;
            transition: 0.25s ease;
            border: 1px solid #202020;
            margin-top: 10px;
            margin-bottom: 10px;
        }

        .chat-history button:hover {
            background-color: #202020;
            cursor: pointer;
        }

        .chat-history button:active {
            background-color: #FFF;
            color: #000;
            cursor: pointer;
        }

        .buttons h1 {
            font-weight: 600;
            margin: 0 10px 10px;
            padding: 0;
        }

        .user-input-div {
            position: fixed;
            background-color: #0c0c0c;
            bottom: 0;
            right: 0;
            display: flex;
            justify-content: center;
            width: 82.5vw;
        }

        .user-input-div textarea {
            background-color: #1b1b1b;
            padding: 5px;
            border: none;
            border-radius: 5px;
            width: 30em;
            margin: 10px;
            color: #FFF;
            resize: none;
            overflow-y: hidden;
            text-align: left;
            box-sizing: border-box;
        }

        .user-input-div textarea::placeholder {
            color: #aaa;
            opacity: 1;
        }

        .nav-btns {
            display: flex;
            justify-content: flex-start;
            flex-direction: row;
            align-items: center;
        }

        .nav-btns .align-left,
        .nav-btns .align-right {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .nav-btns .align-left {
            margin-right: auto;
            margin-left: 15px;
        }

        .nav-btns .align-right {
            margin-left: auto;
            margin-right: 15px;
        }

        .chat-history .add-button {
            padding: 5px;
            width: 2em;
            border-radius: 2.5px;
            color: #FFF;
            font-size: 1.1em;
        }
    </style>
</head>

<body>
    <div style="display: flex; flex-direction: row; justify-content: center;">
        <div class="chat-history">
            <div class="buttons">
                <h1 style="font-weight: 700; margin-top: 10px; margin-left: 15px; font-size: 25px;">
                    Ethereum | A.I.
                </h1>
                <hr style="margin-top:15px; margin-bottom: 20px;">
                <div id="btns">
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                    <button>
                        This is a test
                    </button>
                </div>
            </div>
            <div class="nav-btns">
                <div class="align-left">
                    <p>Version: v1.0.0 [ALPHA]</p>
                </div>
                <div class="align-right">
                    <button class="add-button"><i class="fa-solid fa-plus"></i></button>
                </div>
            </div>
        </div>
        <div class="user-input-div">
            <!-- Change input to textarea -->
            <textarea id="userInput" placeholder="Ask something..."></textarea>
        </div>
    </div>
</body>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const textarea = document.getElementById('userInput');

        function autoResizeTextarea() {
            const maxHeight = 90; // Maximum height in pixels (for about 10 lines)
            textarea.style.height = '2.25em'; // Reset the height
            const newHeight = textarea.scrollHeight; // Calculate the new height

            if (newHeight > maxHeight) {
                textarea.style.height = maxHeight + 'px';
                textarea.style.overflowY = 'scroll'; // Enable scrolling if content exceeds max height
            } else {
                textarea.style.height = newHeight + 'px';
                textarea.style.overflowY = 'hidden'; // Hide scrollbar if within max height
            }
        }

        // Trigger resizing on input
        textarea.addEventListener('input', autoResizeTextarea);

        // Initial resize to fit content
        autoResizeTextarea();
    });
</script>

<script>
    async function getPrediction() {
        document.getElementById('responseContainer').style.display = 'none';
        document.getElementById('loadingAnimation').style.display = 'block';
        document.getElementById('messageText').textContent = '';
        const userInput = document.getElementById('userInput').value;
        const url = new URL('/api/predictions', window.location.origin);
        url.searchParams.append('userInput', userInput);

        const response = await fetch(url);
        const reader = response.body.getReader();
        const decoder = new TextDecoder('utf-8');

        let buffer = '';
        let result = '';
        while (true) {
            const { value, done } = await reader.read();
            if (done) break;
            buffer += decoder.decode(value, { stream: true });
            let lineEndIndex;
            while ((lineEndIndex = buffer.indexOf('\n')) >= 0) {
                const line = buffer.slice(0, lineEndIndex + 1);
                buffer = buffer.slice(lineEndIndex + 1);
                if (line.startsWith('data: ')) {
                    const jsonStr = line.substring(6);
                    try {
                        const jsonObj = JSON.parse(jsonStr);
                        if (jsonObj.choices && jsonObj.choices[0] && jsonObj.choices[0].delta && jsonObj.choices[0].delta.content) {
                            result += jsonObj.choices[0].delta.content;
                        }
                    } catch (error) {
                        console.error('Error parsing JSON:', error);
                    }
                }
            }
        }
        document.getElementById('loadingAnimation').style.display = 'none';
        document.getElementById('messageText').textContent = result;
    }

    document.getElementById('promptForm').addEventListener('submit', function (event) {
        try {
            event.preventDefault();
            document.getElementById('responseContainer').style.display = 'block';
            document.getElementById('loadingAnimation').style.display = 'block';

            getPrediction();

            const welcomeMessage = document.getElementById('welcomeMessage');
            welcomeMessage.classList.remove('fade-in');
            welcomeMessage.classList.add('fade-out');

            document.getElementById('exampleBoxes').classList.add('fade-out');

            setTimeout(function () {
                document.getElementById('responseContainer').style.display = 'block';
            }, 1000);
        } catch (error) {
            console.error('Error:', error);
            alert('Error: ' + error);
        }
    });
</script>

</html>